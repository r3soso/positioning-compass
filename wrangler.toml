# Cloudflare Pages 配置
name = "positioning-compass"
compatibility_date = "2025-06-01"
pages_build_output_dir = "public"

# Pages Functions 目录（Cloudflare 自动识别）
# functions/ 目录中的文件会自动转为 API 路由

# D1 数据库绑定在 Cloudflare Dashboard 中配置：
# Settings → Functions → D1 database bindings → DB → positioning-compass-db
